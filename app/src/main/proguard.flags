# Keep classes and methods that have the @UsedForTesting annotation
-keep @me.sergiotarxz.openboard.inputmethod.annotations.UsedForTesting class *
-keepclassmembers class * {
    @me.sergiotarxz.openboard.inputmethod.annotations.UsedForTesting *;
}

# Keep classes and methods that have the @ExternallyReferenced annotation
-keep @me.sergiotarxz.openboard.inputmethod.annotations.ExternallyReferenced class *
-keepclassmembers class * {
    @me.sergiotarxz.openboard.inputmethod.annotations.ExternallyReferenced *;
}

# Keep native methods
-keepclassmembers class * {
    native <methods>;
}

# Keep classes that are used as a parameter type of methods that are also marked as keep
# to preserve changing those methods' signature.
-keep class me.sergiotarxz.openboard.inputmethod.latin.AssetFileAddress
-keep class me.sergiotarxz.openboard.inputmethod.latin.Dictionary
-keep class me.sergiotarxz.openboard.inputmethod.latin.NgramContext
-keep class me.sergiotarxz.openboard.inputmethod.latin.makedict.ProbabilityInfo
-keep class me.sergiotarxz.openboard.inputmethod.latin.utils.LanguageModelParam

# TODO: remove once used in code.
-keep class me.sergiotarxz.openboard.inputmethod.keyboard.KeyboardLayout { *; }

